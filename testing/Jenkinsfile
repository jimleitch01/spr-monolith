pipeline {
    agent none
    stages {
        stage('Tests') {
            
            parallel {
                stage('JaCoCo') {
                    agent {
                        label "maven"
                    }
                    steps {
                        // checkout scm
                        sh "mvn -B -s /usr/share/maven/ref/repository/settings-docker.xml jacoco:report-aggregate"
                    }
                    // post {
                    //     always {
                    //         junit "**/TEST-*.xml"
                    //     }
                    // }
                }
                stage('Integration') {
                    agent {
                        label "maven"
                    }
                    steps {
                        // checkout scm
                        sh "mvn -B -s /usr/share/maven/ref/repository/settings-docker.xml surefire:test -Dtest=PetclinicIntegrationTests"
                    }
                    // post {
                    //     always {
                    //         junit "**/TEST-*.xml"
                    //     }
                    // }
                }
                stage('Model') {
                    agent {
                        label "maven"
                    }
                    steps {
                        // checkout scm
                        sh "mvn -B -s /usr/share/maven/ref/repository/settings-docker.xml surefire:test -Dtest=ValidatorTests"
                    }
                    // post {
                    //     always {
                    //         junit "**/TEST-*.xml"
                    //     }
                    // }
                }
                stage('Owner') {
                    agent {
                        label "maven"
                    }
                    steps {
                        // checkout scm
                        sh "mvn -B -s /usr/share/maven/ref/repository/settings-docker.xml surefire:test -Dtest=*Controller* -Dtest=*Formatter*" 
                    }
                    // post {
                    //     always {
                    //         junit "**/TEST-*.xml"
                    //     }
                    // }
                }
                stage('Service') {
                    agent {
                        label "maven"
                    }
                    steps {
                        // checkout scm
                        sh "mvn -B -s /usr/share/maven/ref/repository/settings-docker.xml surefire:test  -Dtest=ClinicServiceTests -Dtest=EntityUtils"
                    }
                    // post {
                    //     always {
                    //         junit "**/TEST-*.xml"
                    //     }
                    // }
                }
                stage('System') {
                    agent {
                        label "maven"
                    }
                    steps {
                        // checkout scm
                        sh "mvn -B -s /usr/share/maven/ref/repository/settings-docker.xml surefire:test -Dtest=CrashControllerTests"
                    }
                    // post {
                    //     always {
                    //         junit "**/TEST-*.xml"
                    //     }
                    // }
                }
                stage('Vet') {
                    agent {
                        label "maven"
                    }
                    steps {
                        // checkout scm
                        sh "mvn -B -s /usr/share/maven/ref/repository/settings-docker.xml surefire:test -Dtest=Vet*"
                    }
                    // post {
                    //     always {
                    //         junit "**/TEST-*.xml"
                    //     }
                    // }
                }










                stage('jMeter') {
                    agent {
                        label "maven"
                    }
                    steps {
                        // checkout scm
                        sh "sleep 15"
                    }
                    // post {
                    //     always {
                    //         junit "**/TEST-*.xml"
                    //     }
                    // }
                }
            }
        }
    }
}




