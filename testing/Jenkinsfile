pipeline {
    agent {
        label 'linux'
    }
    stages {
        stage('GitCheckout'){
            steps{
                checkout scm
            }}


        stage('testing 123') {
            steps {
                sh 'echo hello from agent node'
                sh 'mvn mvn -f /tmp/pom.xml -B -s seed/maven/ref/settings-docker.xml -Dtest=PetControllerTests surefire:test  '
            }
        }
    }
}