pipeline {
    agent {
        label 'linux'
    }
    stages {
        stage('GitCheckout'){
            steps{
                checkout scm
            }}


        stage('testing 123') {
            steps {
                sh 'echo hello from agent node'
                sh 'mvn surefire:test -Dtest=PetControllerTests'
            }
        }
    }
}